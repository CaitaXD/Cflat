.\" Auto-generated by cflatdoc
.TH "CFLAT-ARENA" "3" "2026-05-09" "Cflat" "Cflat Arena module"
.SH NAME
cflat\-arena \- Cflat Arena module
.SH DESCRIPTION
Auto-generated API reference extracted from the public header.
.SH FUNCTIONS
.TP
.nf
\fBcflat_arena_init\fR
CflatArena* cflat_arena_init(void *mem, usize size)
.fi
Initializes a new arena in a preallocated region of memory
.PP
\fBParameters\fR
.RS
\fImem\fR
pointer to the memory region
.RE
.RS
\fIsize\fR
size of the memory region
.RE
.TP
.nf
\fBcflat_arena_memory_mapped\fR
CflatArena* cflat_arena_memory_mapped(const char *filepath, usize size_hint, CflatPermission mode)
.fi
Allocates a new arena in a memory mapped file
.PP
\fBParameters\fR
.RS
\fIfilepath\fR
path to the file, relative or absolute
.RE
.RS
\fIsize_hint\fR
minimum size in bytes to allocate for the memory mapped region
.RE
.RS
\fImode\fR
permissions of the memory mapped region
.RE
.TP
.nf
\fBcflat_arena_new_opt\fR
CflatArena* cflat_arena_new_opt(CflatArenaNewOpt opt)
.fi
Allocates a new arena using os specific memory allocation
.PP
\fBParameters\fR
.RS
\fIopt\fR
.RS
\fI.reserve\fR
how much memory should reserved but not yet commited
\fI.commit\fR
how much memory should be commited
\fI.fixed_size\fR
weather or not the arena can gorw by allocate new arena nodes
.RE
.RE
.TP
.nf
\fBcflat_arena_temp_begin\fR
CflatTempArena cflat_arena_temp_begin(CflatArena *arena)
.fi
.TP
.nf
\fBcflat_get_scratch_arena_opt\fR
CflatTempArena cflat_get_scratch_arena_opt(CflatScratchArenaScopeOpt opt)
.fi
.TP
.nf
\fBcflat_arena_try_push_opt\fR
bool cflat_arena_try_push_opt(CflatArena *arena, usize size, void **mem, CflatAllocOpt opt)
.fi
.TP
.nf
\fBcflat_arena_clear\fR
void cflat_arena_clear(CflatArena *arena)
.fi
Clears the arena, making it possible to reuse the memory
.PP
\fBParameters\fR
.RS
\fIarena\fR
the arena
.RE
.TP
.nf
\fBcflat_arena_delete\fR
void cflat_arena_delete(CflatArena *arena)
.fi
Destroys the arena
For each node on the arena
    Frees  memory \- if the node was allocated
    Unmaps memory \- if the node was memory mapped
    Nothing       \- if the node was preallocated
.PP
\fBParameters\fR
.RS
\fIarena\fR
the arena
.RE
.TP
.nf
\fBcflat_arena_pop\fR
void cflat_arena_pop(CflatArena *arena, usize size)
.fi
Deallocates size bytes of memory from the arena
.PP
\fBParameters\fR
.RS
\fIarena\fR
the arena
.RE
.RS
\fIsize\fR
size in bytes to deallocate
.RE
.TP
.nf
\fBcflat_arena_set_pos\fR
void cflat_arena_set_pos(CflatArena *arena, usize pos)
.fi
Sets the arena position to the requested point
Allocates more memory if position is greater than the whats currently in the arena
.PP
\fBParameters\fR
.RS
\fIarena\fR
the arena
.RE
.RS
\fIpos\fR
the position
.RE
.TP
.nf
\fBcflat_arena_temp_end\fR
void cflat_arena_temp_end(CflatTempArena temp_arena)
.fi
.TP
.nf
\fBcflat_drop_scratch_arena\fR
void cflat_drop_scratch_arena(const CflatTempArena temp_arena)
.fi
.TP
.nf
\fBcflat_arena_extend_opt\fR
void* cflat_arena_extend_opt(CflatArena *arena, void *ptr, usize oldsize, usize newsize, CflatAllocOpt opt)
.fi
Takes a region of memory inside the arena and returns a bigger one containing the same information
The region can be the same if theres enough space in the arena but that is not a garantee
.PP
\fBParameters\fR
.RS
\fIarena\fR
the arena
.RE
.RS
\fIptr\fR
the old region of memory
.RE
.RS
\fIoldsize\fR
the size of the old region of memory
.RE
.RS
\fInewsize\fR
the size of the new region of memory
.RE
.RS
\fIopt\fR
.RS
\fI.align\fR
memory alignment of the pointer allocated
\fI.clear\fR
weather or not the allocated memory should be zeroed
.RE
.RE
.PP
\fBReturns\fR
.RS
the new region of memory of size newsize
.RE
.TP
.nf
\fBcflat_arena_push_opt\fR
void* cflat_arena_push_opt(CflatArena *arena, usize size, CflatAllocOpt opt)
.fi
Allocates size bytes of memory from the arena
.PP
\fBParameters\fR
.RS
\fIarena\fR
the arena
.RE
.RS
\fIsize\fR
size in bytes to allocate
.RE
.RS
\fIopt\fR
.RS
\fI.align\fR
memory alignment of the pointer allocated
\fI.clear\fR
weather or not the allocated memory should be zeroed
.RE
.RE
.TP
.nf
\fBcflat_arena_top\fR
void* cflat_arena_top(CflatArena *arena)
.fi
Returns a pointer the end of the arena
The pointer is outside the adress range of the arena
.PP
\fBParameters\fR
.RS
\fIarena\fR
the arena
.RE
.SH TYPES
.TP
\fBCflatAllocOpt\fR
.PP
.RS
.nf
usize align: memory alignment of the pointer allocated
bool  clear: weather or not the allocated memory should be zeroed
.fi
.RE
.TP
\fBCflatArena\fR
.PP
.RS
.nf
struct cflat_arena_node * curr
struct cflat_arena_node * free
CflatArenaFlags           flags
usize                     pos
.fi
.RE
.TP
\fBCflatArenaNewOpt\fR
.PP
.RS
.nf
usize reserve:    how much memory should reserved but not yet commited
usize commit:     how much memory should be commited
bool  fixed_size: weather or not the arena can gorw by allocate new arena nodes
.fi
.RE
.TP
\fBCflatArenaNode\fR
.PP
.RS
.nf
CflatArena                _arena
struct cflat_arena_node * prev
usize                     pos
usize                     res
usize                     cmt
byte                      data
.fi
.RE
.TP
\fBCflatScratchArenaScopeOpt\fR
.PP
.RS
.nf
usize         conflicts_count: number of conflicting arenas
CflatArena ** conflicts:       array of arenas that should be filtered out
.fi
.RE
.TP
\fBCflatTempArena\fR
.PP
.RS
.nf
CflatArena * arena
usize        pos
.fi
.RE
.TP
\fBCflatArenaFlags\fR : u64
.PP
.RS
.nf
\fBCFLAT_ARENA_OWNS_MEMORY\fR  1 << 0
\fBCFLAT_ARENA_FIXED_SIZE \fR  1 << 1
\fBCFLAT_MEMORY_MAPPED    \fR  1 << 2
.fi
.RE
.TP
\fBCflatPermission\fR : u8
.PP
.RS
.nf
\fBCFLAT_PERMISSION_READ   \fR  1 << 0
\fBCFLAT_PERMISSION_WRITE  \fR  1 << 1
\fBCFLAT_PERMISSION_EXECUTE\fR  1 << 2
.fi
.RE
.SH OPTS
.TP
.nf
void* cflat_arena_extend(CflatArena *arena, void *ptr, usize oldsize, usize newsize, usize align=cflat_alignof(uptr), bool clear=false)
.fi
.RS
.nf
Caller:
  cflat_arena_extend(arena, ptr, oldsize, newsize, .align=align, .clear=clear)
  cflat_arena_extend(arena, ptr, oldsize, newsize)

Function:
  void* cflat_arena_extend_opt (CflatArena *arena, void *ptr, usize oldsize, usize newsize, CflatAllocOpt opt)
.fi
.RE
.TP
.nf
CflatArena* cflat_arena_new(usize reserve=CFLAT_DEFAULT_RESERVE_SIZE, usize commit=CFLAT_DEFAULT_COMMIT_SIZE, bool fixed_size=false)
.fi
.RS
.nf
Caller:
  cflat_arena_new(.reserve=reserve, .commit=commit, .fixed_size=fixed_size)
  cflat_arena_new()

Function:
  CflatArena* cflat_arena_new_opt (CflatArenaNewOpt opt)
.fi
.RE
.TP
.nf
void* cflat_arena_push(CflatArena *arena, usize size, usize align=cflat_alignof(uptr), bool clear=false)
.fi
.RS
.nf
Caller:
  cflat_arena_push(arena, size, .align=align, .clear=clear)
  cflat_arena_push(arena, size)

Function:
  void* cflat_arena_push_opt (CflatArena *arena, usize size, CflatAllocOpt opt)
.fi
.RE
.TP
.nf
void* cflat_arena_push_array(Type(T), CflatArena *arena, usize n, usize align=cflat_alignof(T), bool clear=false)
.fi
.RS
.nf
Caller:
  cflat_arena_push_array(T, arena, n, .align=align, .clear=clear)
  cflat_arena_push_array(T, arena, n)

Function:
  void* cflat_arena_push_opt (CflatArena *arena, usize size, CflatAllocOpt opt)
.fi
.RE
.TP
.nf
void* cflat_arena_push_ptr(CflatArena *arena, usize align=cflat_alignof(uptr), bool clear=false)
.fi
.RS
.nf
Caller:
  cflat_arena_push_ptr(arena, .align=align, .clear=clear)
  cflat_arena_push_ptr(arena)

Function:
  void* cflat_arena_push_opt (CflatArena *arena, usize size, CflatAllocOpt opt)
.fi
.RE
.TP
.nf
void* cflat_arena_push_struct(Type(T), CflatArena *arena, usize align=cflat_alignof(T), bool clear=false)
.fi
.RS
.nf
Caller:
  cflat_arena_push_struct(T, arena, .align=align, .clear=clear)
  cflat_arena_push_struct(T, arena)

Function:
  void* cflat_arena_push_opt (CflatArena *arena, usize size, CflatAllocOpt opt)
.fi
.RE
.TP
.nf
bool cflat_arena_try_push(CflatArena *arena, usize size, void **mem, usize align=cflat_alignof(uptr), bool clear=false)
.fi
.RS
.nf
Caller:
  cflat_arena_try_push(arena, size, mem, .align=align, .clear=clear)
  cflat_arena_try_push(arena, size, mem)

Function:
  bool cflat_arena_try_push_opt (CflatArena *arena, usize size, void **mem, CflatAllocOpt opt)
.fi
.RE
.TP
.nf
CflatTempArena cflat_get_scratch_arena(usize conflicts_count=0)
.fi
.RS
.nf
Caller:
  cflat_get_scratch_arena(.conflicts_count=conflicts_count)
  cflat_get_scratch_arena()

Function:
  CflatTempArena cflat_get_scratch_arena_opt (CflatScratchArenaScopeOpt opt)
.fi
.RE
.SH SCOPES
.TP
\fBcflat_scratch_arena_scope(tmp, usize conflicts_count=0)\fR
.RS
.nf
Caller:
  cflat_scratch_arena_scope(tmp, .conflicts_count=conflicts_count) {
    ...
  }
  cflat_scratch_arena_scope(tmp) {
    ...
  }

Function:
  CflatTempArena cflat_get_scratch_arena_opt (CflatScratchArenaScopeOpt opt)
  void cflat_drop_scratch_arena (const CflatTempArena temp_arena)
.fi
.RE
.TP
\fBcflat_temp_arena_scope(arena)\fR
.RS
.nf
Caller:
  cflat_temp_arena_scope(arena) {
    ...
  }

Function:
  CflatTempArena cflat_arena_temp_begin (CflatArena *arena)
  void cflat_arena_temp_end (CflatTempArena temp_arena)
.fi
.RE
.SH MACROS
.TP
\fBCFLAT_ARRAY_SPLAT(ARRAY)\fR
.TP
\fBCFLAT_DEFAULT_COMMIT_SIZE (KiB(4))\fR
.TP
\fBCFLAT_DEFAULT_RESERVE_SIZE (KiB(64))\fR
.TP
\fBcflat_clear_flag(FLAGS, FLAG)\fR
.TP
\fBcflat_has_flag(FLAGS, FLAG)\fR
.TP
\fBcflat_set_flag(FLAGS, FLAG)\fR
.TP
\fBcflat_stackalloc(SIZE)\fR
.SH SEE ALSO
\fBcflat\fR(3)
